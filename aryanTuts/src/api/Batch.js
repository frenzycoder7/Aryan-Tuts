const batch = require('express').Router();
const Batch = require('./../Models/Batch');
batch.get('/all-batch',async(req,res)=>{
    try {
        
    } catch (error) {
        
    }
})

module.exports = batch;